pipeline {
    agent any

    tools {
        maven "MAVEN_HOME"
    }

    stages {
        stage ('DAST') {
            steps {
                sh '''
                docker run -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://www.vulnweb.com -r zap-baseline-scan.html || true
                '''
            }
        }
    }
}