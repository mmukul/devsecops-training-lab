pipeline {
    agent any

    stages {
        stage('Start ZAP Scan') {
            steps {
                echo 'Starting OWASP ZAP security scan...'
                
                sh '''
                docker run -v $(pwd):/zap/wrk/:rw -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://www.vulnweb.com -g gen.conf -r zap-report.html -f openapi || true
                '''
            }
        }
    }

    post {
        always {
            echo 'Publishing ZAP Security Report...'

            publishHTML (target: [
                reportFiles: 'zap-report.html',
                reportName: 'OWASP ZAP Security Report',
                keepAll: true,
                alwaysLinkToLastBuild: true,
                allowMissing: false
            ])
        }
    }
}